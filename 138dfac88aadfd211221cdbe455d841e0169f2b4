Here is the rewritten commit message in Conventional Commits format:

```
feat: switch to python:3.12-slim as base image and update dependencies layer

This commit updates the Dockerfile to use Python 3.12 slim as the base image,
and refactors the dependencies layer for improved performance.

Changes include:
- Adding `pool_pre_ping=True` to validate database connections
- Removing unnecessary apt-get commands
- Simplifying pip installation commands
- Updating the runtime image to use Python 3.12 slim

Also updated .dockerignore files and frontend Dockerfile.
```
